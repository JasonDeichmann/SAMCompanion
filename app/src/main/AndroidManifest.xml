<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kayletiu.samcompanion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Companion"></activity>
        <activity android:name=".GamesActivity.GamesActivity"></activity>
        <activity android:name=".GamesActivity.InGame"/>
        <activity
            android:name=".AddPost"
            android:label="@string/title_activity_add_post"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ReplyPost"
            android:label="@string/title_activity_reply_post"
            android:theme="@style/AppTheme" />
    </application>

</manifest>